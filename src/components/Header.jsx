function Header(){
    return(
        <header>
            <h2>Mi primera pagina</h2>
            <nav>
                <a href="#inicio">Inicio</a> | <a href="#Servicios"></a>
            </nav>
        </header>
    );
};

export default Header;